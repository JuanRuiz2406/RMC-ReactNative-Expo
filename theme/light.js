import { DefaultTheme } from '@react-navigation/native'

export const lightTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        background: 'rgb(242, 242, 242)',

    },
};